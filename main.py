from app import  database, crud, models, queries

if __name__ == "__main__":
        
    def main():
        # Exemplo de utilização das funções CRUD
        impressora_id = 1
        contador_pb = 100
        contador_cor = 50
        contador_total = contador_pb + contador_cor
        data_leitura = datetime.datetime.now()
        created_at = datetime.datetime.now()

        # crud.create_contagem_impressoras(impressora_id, contador_pb, contador_cor, contador_total, data_leitura, created_at)
    
        
    
